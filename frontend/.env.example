# Used server-side only by the Vercel serverless function api/jobs.js (JSearch/RapidAPI proxy).
# Not prefixed with VITE_, so it is never exposed to the client bundle.
# NOTE: pending removal — see Task 9 of the backend migration, api/jobs.js is being retired.
JSEARCH_KEY=

# Base URL of the FastAPI backend (see backend/README.md).
VITE_API_BASE_URL=
